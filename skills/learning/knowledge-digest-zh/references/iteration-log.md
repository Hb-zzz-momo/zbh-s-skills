## 2026-04-23 迭代 #1

- **触发反馈**：用户指出在“按链路理解代码”的教学里，函数上下游没有直观联系，函数层讲解没有对应链路传递关系。
- **根因**：原规则强调知识点、步骤链和工作流，但没有把“函数节点之间的真实调用关系与传递物”写成代码链路任务的硬约束，导致输出容易退化为函数清单。
- **修改内容**：在 `SKILL.md` 新增“链路型代码任务附加规则”；在模式B新增“链路传递拆解”追加结构；在模式B质量标准中补充“必须显式写出上游→当前→下游和传递对象”。
- **影响范围**：模式B / 代码链路类总结 / 自我迭代规范

## 2026-04-24 迭代 #2

- **触发反馈**：用户要求“最终输出一个总结文件夹”。
- **根因**：原规则默认正式输出为单个 Markdown 文件，不利于长期任务中把摘要、深度知识、自测题、可复用工作流分开归档。
- **修改内容**：将正式交付要求改为在 `skill-outputs/` 下新建总结文件夹；新增模式 A/B/C 的默认文件夹结构；明确 `README.md`、`学习摘要.md`、`知识总结.md`、`可复用工作流.md`、`自测题.md`、`参考答案.md` 的使用场景。
- **影响范围**：Markdown 交付要求 / 模式A / 模式B / 模式C / 最终回复说明

## 2026-05-11 迭代 #3

- **触发反馈**：与 `artifact-curator-zh` 策展规范对齐，`skill-outputs` 按技能分子目录。
- **根因**：根目录平铺过多文件时不利于索引与按技能批量归档。
- **修改内容**：默认保存根改为 `skill-outputs/knowledge-digest-zh/`；模式 A/B/C 树状示例同步；最终回复路径须含该前缀；写明与 **legacy-flat**（历史根目录交付）兼容并存。
- **影响范围**：Markdown 交付要求 / 模式A / 模式B / 模式C / 最终回复说明
